
<?php
  
    $name = "SamyogKoirala";
    $phone = "981-932-0008";

    // $name = strtolower($name); // ! To Convert the String to lower
    // $name = strtoupper($name); // ! To Convert the String to lower
    // $name = trim($name); // ! To Remove all the extra spaces
    // $name = str_pad($name, 20, "0"); //! Fill the extra space with "0"
    // $name = str_replace("-", "#", $phone); // ! Remove the - with # value
    // $name = strrev($name); // ! Reverse the String like: Samyog => goymaS 
    // $name = str_shuffle($name); // ! Create random shuffel between variable's value
    // $name = strcmp($name, "A"); // ! Compare the first letter of first string with first letter of second like A = 67 and S = 83 so result will be 18
    // $name = strlen($name); // ! Return the count of the Variable
    // $name = strpos($phone,"-"); // ! Search the element and return its index
    // $name = substr($name, 0, 6); // ! Trim the variable from 0 index to 6th (variableName, startIndex, endIndex);
    // $name = explode(" ", $name); // ! Converts normal variable into array
    // $name = implode("--->", $name); // ! Converts array into normal variable

    echo $name;

?>